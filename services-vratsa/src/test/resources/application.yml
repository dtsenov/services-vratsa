spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: test
    password: bgalert
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

services-vratsa:
  remember.me.key: bgalert

cloudinary:
  api-key: 121212
  api-secret: verysecret
  cloud-name: myname
